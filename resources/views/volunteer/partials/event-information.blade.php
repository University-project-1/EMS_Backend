<section class="volunteer-event-card" aria-labelledby="event-information-title">
    <h2 id="event-information-title">{{ __('volunteer.page.information_title') }}</h2>
    <div class="volunteer-facts">
        <article class="volunteer-fact">
            <strong>{{ __('volunteer.event.period_label') }}</strong>
            <span>{{ __('volunteer.event.period') }}</span>
        </article>
        <article class="volunteer-fact">
            <strong>{{ __('volunteer.event.hours_label') }}</strong>
            <span>{{ __('volunteer.event.hours') }}</span>
        </article>
        <article class="volunteer-fact">
            <strong>{{ __('volunteer.event.location_label') }}</strong>
            <span>{{ __('volunteer.event.location') }}</span>
        </article>
    </div>
</section>
